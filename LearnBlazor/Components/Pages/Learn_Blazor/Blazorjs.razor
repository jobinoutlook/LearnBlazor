@page "/blazorjs"
@inject IJSRuntime _JSRuntime
<h3>Blazor JS</h3>

<div class="row">
    <button class="btn btn-secondary" style="width:auto" @onclick="TestConfirmBox">Test Confirm Button</button> <br />
</div>

<div class="row mt-1">
   
    @if(ConfirmResult)
    {
        
        <p>Confirmation has been made</p>
    }
    else
    {
        
        <p>Pending Confirmation</p>
    }
</div>

<div class="row">
    <button class="btn btn-success m-2" style="width:auto" @onclick="ToastrSuccess">Toastr Success</button>
    <button class="btn btn-danger m-2" style="width:auto" @onclick="ToastrError">Toastr Failure</button>
</div>

<div class="row">
    <button class="btn btn-success m-2" style="width:auto" @onclick="SweetalertSuccess">Sweetalert Success</button>
    <button class="btn btn-danger m-2" style="width:auto" @onclick="SweetalertError">Sweetalert Failure</button>
</div>

@code {

    private string ConfirmMesssage = "Are you sure want to click here?";

    bool ConfirmResult{ get; set; }

    private async Task TestConfirmBox()
    {
        //ConfirmResult = await _JSRuntime.InvokeAsync<bool>("confirm", ConfirmMesssage);
        ConfirmResult = await _JSRuntime.ConfirmBox(ConfirmMesssage);
    }
    //------

    private async Task ToastrSuccess()
    {
        //await _JSRuntime.InvokeVoidAsync("showToastr","success","Success Notification");
        await _JSRuntime.ToastrSuccess("Success Notification");
    }

    private async Task ToastrError()
    {
        //await _JSRuntime.InvokeVoidAsync("showToastr", "error", "Error Notification");
        await _JSRuntime.ToastrError("Error Notification");
    }
    //---------
    private async Task SweetalertSuccess()
    {
        //await _JSRuntime.InvokeVoidAsync("showToastr","success","Success Notification");
        await _JSRuntime.SwalSuccess("Success Notification");
    }

    private async Task SweetalertError()
    {
        //await _JSRuntime.InvokeVoidAsync("showToastr", "error", "Error Notification");
        await _JSRuntime.SwalError("Error Notification");
    }

}
